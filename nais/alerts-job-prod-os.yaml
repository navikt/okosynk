apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: okosynkos
  namespace: okonomi
  labels:
    team: okonomi
spec:
  groups:
    - name: okosynk-os-alerts
      rules:
        - alert: okosynk-OS sender varsel
          expr: okosynk_os_batch_alert > 0
          for: 1m
          annotations:
            action: "Sjekk hvorfor okosynk gir varsel"
            summary: "okosynk-os gir varsel. Sjekk hvorfor"
          labels:
            namespace: okonomi
            severity: danger
            okosynk_os_type: okosynkos
            alert_type: custom
        - alert: okosynk-OS har opprettet mange oppgaver
          expr: okosynk_job_oppgaver_opprettet{cronjob="bokosynk001"} > 5000
          for: 1m
          annotations:
            action: "Sjekk om noe spesielt har skjedd som gjør at utbetalinger ikke går gjennom"
            summary: "okosynk-OS har opprettet over 5000 oppgaver"
          labels:
            namespace: okonomi
            severity: danger
            okosynk_ur_type: okosynkos
            alert_type: custom
---

apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: okosynk-os-slack
  namespace: okonomi
  labels:
    alertmanagerConfig: okosynk-os-slack
spec:
  receivers:
    - name: okosynk-os-receiver
      slackConfigs:
        - apiURL:
            key: apiUrl
            name: slack-webhook
          channel: '#team-mob-alerts-prod'
          httpConfig:
            proxyURL: http://webproxy.nais:8088
          iconEmoji: ':alert:'
          username: 'Alert prod-fss'
          sendResolved: true
          title: |-
            [{{ .Status | toUpper }}{{ if eq .Status "firing" }}:{{ .Alerts.Firing | len }}{{ end }}] {{ .CommonLabels.alertname }}
          text: >-
            {{ range .Alerts }}
            {{ if or .Annotations.summary .Annotations.message }}
            {{ or .Annotations.summary .Annotations.message }}
            {{ println " " }}
            {{- end }}
            {{- if .Annotations.action }}
            • *action*: {{ .Annotations.action }} {{ println " " }}
            {{- end }}
            {{ end }}
  route:
    groupBy:
      - alertname
    matchers:
      - name: "okosynk_os_type"
        matchType: "="
        value: "okosynkos"
    groupInterval: 10s
    groupWait: 5s
    receiver: okosynk-os-receiver
    repeatInterval: 1440m

